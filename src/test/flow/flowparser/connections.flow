version 0.1

flow Connections0 {
    ;
    in.;
    in.->;
    ->;
    ->.out;
}

flow Connections1 {
    ->(doIt);
    in.->(doIt);
    (doIt)->;
    (doIt)->.out;
    ->(doIt)->;
    in.->in.(doIt).out->.out;
    ->(doIt)->(doThis)->(doThat);
    in.->(doIt).out->(doThis)->in.(doThat)->.out;
    ->in.(doIt)->in.(doThat).out->(doThis).out->;
    (doThis).err->.err;
    in.->in2.(doThis);
}
