package ;


public class Connections1 {
    private final DoIt doIt;
    private final DoThis doThis;
    private final DoesThat doThat;
    private final DoesThat doesThat;

    public Connections1() {
        doIt = new DoIt();
        doThis = new DoThis();
        doThat = new DoesThat();
        doesThat = new DoesThat();

        createConnections();
        initConfig();
    }

    private void createConnections() {
        doIt.setPort(doThis.getPort());
        doThis.setPort(doThat.getPort());
        doIt.setPort(doThis.getPort());
        doThis.setPort(doThat.getPort());
        doIt.setPort(doThat.getPort());
        doThat.setPort(doThis.getPort());
    }

    private void initConfig() {
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort() {
        return doThis.getPort();
    }

    public Port<> getPort() {
        return doesThat.getPort();
    }

    public void setPort(Port<> port) {
        doIt.setPort(port);
    }
    public void setPort(Port<> port) {
        doIt.setPort(port);
    }
    public void setPort(Port<> port) {
        doIt.setPort(port);
    }
    public void setPort(Port<> port) {
        doIt.setPort(port);
    }
    public void setPort(Port<> port) {
        doThat.setPort(port);
    }
    public void setPort(Port<> port) {
        doThis.setPort(port);
    }
    public void setPort(Port<> port) {
        doThis.setPort(port);
    }
    public void setPort(Port<> port) {
        doesThat.setPort(port);
    }
}

package ;


public class NumberedPorts {
    private final  doIt;
    private final  doThis;
    private final  doThat;

    public NumberedPorts() {
        doIt = new ();
        doThis = new ();
        doThat = new ();

        createConnections();
        initConfig();
    }

    private void createConnections() {
        doIt.setPort(doThis.getPort());
        doThis.setPort(doThat.getPort());
        doIt.setPort(0, doThis.getPort(0));
        doThis.setPort(0, doThat.getPort(0));
        doIt.setPort(1, doThis.getPort(1));
        doThis.setPort(1, doThat.getPort(1));
        doIt.setPort(2, doThis.getPort(2));
        doThis.setPort(2, doThat.getPort(2));
    }

    private void initConfig() {
    }

    public Port<> getPort() {
        return doIt.getPort();
    }

    public Port<> getPort(int i) {
        return doIt.getPort(i);
    }

    public Port<> getPort(int i) {
        return doIt.getPort(i);
    }

    public Port<> getPort(int i) {
        return doIt.getPort(i);
    }

    public void setPort(Port<> port) {
        doThat.setPort(port);
    }
    public void setPort(int i, Port<> port) {
        doThat.setPort(i, port);
    }
    public void setPort(int i, Port<> port) {
        doThat.setPort(i, port);
    }
    public void setPort(int i, Port<> port) {
        doThat.setPort(i, port);
    }
}

